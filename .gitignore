/.phpunit.cache
/bootstrap/ssr
/node_modules
# public/build is committed on purpose: the Plesk/IIS target has no Node
# toolchain, so the compiled assets ship with the repository.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# SQLite database — rebuild with `php artisan migrate --seed`
/database/*.sqlite
/database/*.sqlite-journal

# Legacy React + Express build, kept for reference only
/legacy/node_modules
